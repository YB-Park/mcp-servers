# mcp-servers

Internal-first, VS Code-first framework and runtime for building and operating remote MCP servers over HTTP.

> This repository is being bootstrapped. The initial framework, reference server, architecture documents, and test harness are developed on a feature branch before being merged into `main`.
